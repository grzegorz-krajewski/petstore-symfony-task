parameters:
    petstore_api_base_url: '%env(PETSTORE_API_BASE_URL)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'

    App\Service\PetstoreClient:
        arguments:
            $petstoreApiBaseUrl: '%petstore_api_base_url%'